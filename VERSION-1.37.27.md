# JamdDmaj AI v1.37.27

- The app now saves Bitget trade limits before running, refreshing, or queuing a Bitget test, so mobile input edits do not run with old values.
- The VPS executor reports the effective `maxOpen` and `maxNewOrdersPerRun` values in status and logs.
- The Pro scanner screen now shows when the app-configured max open differs from the VPS effective max open.
