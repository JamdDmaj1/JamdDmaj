# JamdDmaj AI v1.37.33

- Fixes the 24/7 server Refresh button so it only reloads server state and does not rewrite or disable the scanner configuration.
- Keeps Bitget executor limits untouched when checking the latest VPS heartbeat from the app.
