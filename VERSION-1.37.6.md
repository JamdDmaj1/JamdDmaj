# JamdDmaj AI v1.37.6

- Adds a client Bitget connector setup panel in Pro mode.
- Adds a client connector feed so private client executors do not need the owner cron secret.
- Updates the VPS executor with JAMDDMAJ_CLIENT_CONNECTOR=true mode.
- Keeps customer Bitget API keys out of JamdDmaj Cloud; the app only generates local setup text for a private connector.
